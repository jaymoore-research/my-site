# my-site

Minimal personal homepage.

## Edit

- Main page: `index.html`
- Jekyll disabled via `.nojekyll` (helps GitHub Pages serve the HTML as-is).

## Deploy (GitHub Pages)

If you’re using GitHub Pages, configure it to serve from the repository root (or your preferred source) and it will pick up `index.html`.
